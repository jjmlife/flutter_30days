import 'package:flutter/material.dart';

class TextControllerDemo extends StatelessWidget {
  const TextControllerDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
